import React from 'react' // Importing the React library
import AddProductForm from '../components/addProductForm/AddProductForm' // Importing the AddProductForm component

// Define the AddProduct component
const AddProduct = () => {
  return (
    <AddProductForm /> // Render the AddProductForm component
  )
}

export default AddProduct // Export the AddProduct component as the default export